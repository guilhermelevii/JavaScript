function verificar(){
    var data = new Date()
    var ano = data.getFullYear()
    var fano = document.getElementById('ano')
    var res = document.querySelector('res') // confirmar se está funcionando no codigo dos estudos é um ID e aqui no caso coloquei como CLASS.
}